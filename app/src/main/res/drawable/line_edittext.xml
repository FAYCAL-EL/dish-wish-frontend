<!-- res/drawable/bottom_line.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color (if needed) -->
    <item android:drawable="@android:color/transparent" />

    <!-- Bottom line -->
    <item
        android:top="22dp"
        android:bottom="1dp"
        android:left="-2dp"
        android:right="-2dp">
    <shape android:shape="rectangle">
        <solid android:color="@color/principalColor" /> <!-- Line color -->
    </shape>
</item>
    </layer-list>
